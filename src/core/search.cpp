#include "search.h"

using namespace std;

void search() {
	cout << "Nhap ten mat hang: ";
	string query;
	cin >> query;
	list<Item>::iterator it;
	for (it = listOfItems.begin(); it != listOfItems.end(); it++) {

	}
}
